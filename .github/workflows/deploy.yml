name: Deploy to VPS

# Se déclenche à chaque push sur main (ou manuellement depuis l'onglet Actions).
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Déploiement SSH sur le VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            set -e
            # `git pull` se fait à la racine du dépôt, mais l'app web+serveur
            # vit dans le sous-dossier MyPlayLog/ (le dépôt sert aussi de
            # parapluie au projet mobile, qui a son propre dépôt).
            cd ~/myplaylog
            git pull origin main
            cd MyPlayLog
            docker compose up -d --build
            docker image prune -f
